/* eslint-disable import/prefer-default-export */
import styled from 'styled-components';

import { FlexColumn } from '@/styles/layout';

export const MecaWriteWrapper = styled.div`
  width: 100%;
  ${FlexColumn};
`;
